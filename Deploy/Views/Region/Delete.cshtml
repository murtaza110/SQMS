@model SQMS.Web.Models.Region

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Region</legend>

    <div class="display-label">RegionType</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.RegionType.RegionTypeName)
    </div>

    <div class="display-label">RegionName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.RegionName)
    </div>

    <div class="display-label">Region1</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Region1.RegionName)
    </div>

    <div class="display-label">IsActive</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.IsActive)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
