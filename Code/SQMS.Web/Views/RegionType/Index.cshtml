@model IEnumerable<SQMS.Web.Models.RegionType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            RegionTypeName
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RegionTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RegionTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RegionTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RegionTypeId })
        </td>
    </tr>
}

</table>
